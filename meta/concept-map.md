# Concept ↔ Use-Case Map

Living matrix linking concepts (rows) to use cases (columns). Cell values: `I` introduced,
`R` reinforced, `A` applied. Ensures no orphan concepts and no use case relying on an untaught
concept. See ../plan.md §4.2.

> **Pending use cases.** Use cases haven't been supplied yet, so the columns are empty. The
> Foundations path is intentionally use-case-agnostic and proceeds without them; re-anchor once
> use cases land.

| Concept | (use-case columns added here) |
|---------|-------------------------------|
| _filled once use cases land_ | |
